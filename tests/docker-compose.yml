services:
  nginx:
    image: nginx
    volumes:
      - "../files/feredirects.conf:/etc/nginx/conf.d/default.conf"
  apache:
    image: httpd
    volumes:
      - "./apache_redirects.conf:/etc/apache2/sites-enabled/default.conf"
  tester:
    image: python:3
    command: [ "python", "tester.py" ]
